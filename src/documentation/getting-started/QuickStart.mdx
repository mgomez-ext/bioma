import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Bioma 2.1/Getting Started/Quick Start" />

# Quick Start

Crea tu primera aplicación con Bioma en 5 minutos.

---

## 🚀 Inicio Rápido

### Paso 1: Crea un Componente Simple

```typescript
// hello-bioma.component.ts
import { Component } from '@angular/core';
import { ButtonComponent } from '@mgomezw/bioma';

@Component({
  selector: 'app-hello-bioma',
  standalone: true,
  imports: [ButtonComponent],
  template: `
    <div class="container">
      <h1>¡Hola Bioma!</h1>
      <p>Mi primera aplicación con Bioma Design System</p>

      <bio-button
        label="Click Me"
        variant="filled"
        size="large"
        (clicked)="handleClick()">
      </bio-button>
    </div>
  `,
  styles: [`
    .container {
      padding: var(--spacing-8);
      max-width: 600px;
      margin: 0 auto;
    }

    h1 {
      color: var(--text-primary);
      margin-bottom: var(--spacing-2);
    }

    p {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-6);
    }
  `]
})
export class HelloBiomaComponent {
  handleClick() {
    console.log('¡Botón clickeado!');
  }
}
```

### Paso 2: Úsalo en tu App

```typescript
// app.component.ts
import { Component } from '@angular/core';
import { HelloBiomaComponent } from './hello-bioma.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [HelloBiomaComponent],
  template: '<app-hello-bioma></app-hello-bioma>'
})
export class AppComponent {}
```

### Paso 3: ¡Ejecuta!

```bash
ng serve
```

Abre `http://localhost:4200` y verás tu primer componente con Bioma. 🎉

---

## 📝 Ejemplos Comunes

### Ejemplo 1: Formulario de Login

```typescript
import { Component } from '@angular/core';
import { ButtonComponent } from '@mgomezw/bioma';
// import { InputComponent } from '@mgomezw/bioma';  // Próximamente

@Component({
  selector: 'app-login',
  standalone: true,
  imports: [ButtonComponent],
  template: `
    <form class="login-form">
      <h2>Iniciar Sesión</h2>

      <!-- Inputs próximamente -->
      <div class="form-group">
        <label>Email</label>
        <input type="email" placeholder="tu@email.com">
      </div>

      <div class="form-group">
        <label>Contraseña</label>
        <input type="password" placeholder="••••••••">
      </div>

      <bio-button
        label="Iniciar Sesión"
        variant="filled"
        size="large"
        [fullWidth]="true"
        (clicked)="onSubmit()">
      </bio-button>
    </form>
  `,
  styles: [`
    .login-form {
      background: var(--bg-default);
      padding: var(--spacing-8);
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-md);
      max-width: 400px;
      margin: var(--spacing-10) auto;
    }

    h2 {
      color: var(--text-primary);
      margin-bottom: var(--spacing-6);
      text-align: center;
    }

    .form-group {
      margin-bottom: var(--spacing-4);
    }

    label {
      display: block;
      color: var(--text-primary);
      font-size: var(--font-size-sm);
      font-weight: var(--font-weight-medium);
      margin-bottom: var(--spacing-2);
    }

    input {
      width: 100%;
      padding: var(--spacing-3);
      border: 1px solid var(--border-default);
      border-radius: var(--radius-md);
      font-size: var(--font-size-base);
      color: var(--text-primary);
      background: var(--bg-default);
      transition: all var(--transition-fast);
    }

    input:focus {
      outline: none;
      border-color: var(--primary-default);
      box-shadow: 0 0 0 3px var(--focus-ring);
    }
  `]
})
export class LoginComponent {
  onSubmit() {
    console.log('Form submitted');
  }
}
```

### Ejemplo 2: Tarjeta de Producto

```typescript
import { Component } from '@angular/core';
import { ButtonComponent } from '@mgomezw/bioma';

@Component({
  selector: 'app-product-card',
  standalone: true,
  imports: [ButtonComponent],
  template: `
    <article class="product-card">
      <div class="product-image">
        <img src="https://via.placeholder.com/300x200" alt="Producto">
      </div>

      <div class="product-content">
        <h3 class="product-title">Nombre del Producto</h3>
        <p class="product-description">
          Descripción breve del producto con sus características principales.
        </p>
        <div class="product-price">$99.99</div>
      </div>

      <div class="product-actions">
        <bio-button
          label="Agregar al Carrito"
          variant="filled"
          [fullWidth]="true"
          [iconStart]="true"
          iconStartName="shopping_cart">
        </bio-button>

        <bio-button
          label="Ver Detalles"
          variant="outlined"
          [fullWidth]="true"
          [iconEnd]="true"
          iconEndName="arrow_forward">
        </bio-button>
      </div>
    </article>
  `,
  styles: [`
    .product-card {
      background: var(--bg-default);
      border: 1px solid var(--border-default);
      border-radius: var(--radius-lg);
      overflow: hidden;
      transition: all var(--transition-normal);
      max-width: 300px;
    }

    .product-card:hover {
      box-shadow: var(--shadow-lg);
      transform: translateY(-4px);
    }

    .product-image img {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .product-content {
      padding: var(--spacing-4);
    }

    .product-title {
      color: var(--text-primary);
      font-size: var(--font-size-lg);
      font-weight: var(--font-weight-semibold);
      margin-bottom: var(--spacing-2);
    }

    .product-description {
      color: var(--text-secondary);
      font-size: var(--font-size-sm);
      margin-bottom: var(--spacing-3);
    }

    .product-price {
      color: var(--primary-default);
      font-size: var(--font-size-xl);
      font-weight: var(--font-weight-bold);
    }

    .product-actions {
      padding: var(--spacing-4);
      display: flex;
      flex-direction: column;
      gap: var(--spacing-2);
    }
  `]
})
export class ProductCardComponent {}
```

### Ejemplo 3: Header con Modo Oscuro

```typescript
import { Component, signal } from '@angular/core';
import { ButtonComponent } from '@mgomezw/bioma';

@Component({
  selector: 'app-header',
  standalone: true,
  imports: [ButtonComponent],
  template: `
    <header class="app-header">
      <div class="header-content">
        <div class="logo">Bioma App</div>

        <nav class="nav">
          <a href="#" class="nav-link">Inicio</a>
          <a href="#" class="nav-link">Productos</a>
          <a href="#" class="nav-link">Contacto</a>
        </nav>

        <bio-button
          [label]="isDark() ? 'Modo Claro' : 'Modo Oscuro'"
          variant="outlined"
          size="medium"
          [iconStart]="true"
          [iconStartName]="isDark() ? 'light_mode' : 'dark_mode'"
          (clicked)="toggleTheme()">
        </bio-button>
      </div>
    </header>
  `,
  styles: [`
    .app-header {
      background: var(--bg-default);
      border-bottom: 1px solid var(--border-default);
      padding: var(--spacing-4) 0;
    }

    .header-content {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 var(--spacing-4);
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo {
      font-size: var(--font-size-xl);
      font-weight: var(--font-weight-bold);
      color: var(--primary-default);
    }

    .nav {
      display: flex;
      gap: var(--spacing-6);
    }

    .nav-link {
      color: var(--text-primary);
      text-decoration: none;
      font-weight: var(--font-weight-medium);
      transition: color var(--transition-fast);
    }

    .nav-link:hover {
      color: var(--primary-default);
    }
  `]
})
export class HeaderComponent {
  isDark = signal(false);

  toggleTheme() {
    this.isDark.update(v => !v);
    const theme = this.isDark() ? 'dark' : 'light';
    document.documentElement.setAttribute('data-theme', theme);
  }
}
```

---

## 🎨 Usando Tokens de Diseño

### Colores

```scss
// Textos
color: var(--text-primary);     // Texto principal
color: var(--text-secondary);   // Texto secundario
color: var(--text-tertiary);    // Texto terciario
color: var(--text-disabled);    // Texto deshabilitado

// Backgrounds
background: var(--bg-default);  // Fondo principal
background: var(--bg-subtle);   // Fondo sutil
background: var(--bg-muted);    // Fondo atenuado

// Acciones
color: var(--primary-default);  // Acción primaria
color: var(--secondary-default);// Acción secundaria

// Estados
color: var(--success-default);  // Éxito
color: var(--warning-default);  // Advertencia
color: var(--error-default);    // Error
```

### Espaciado

```scss
// Padding / Margin
padding: var(--spacing-2);    // 8px
padding: var(--spacing-4);    // 16px
padding: var(--spacing-6);    // 24px
padding: var(--spacing-8);    // 32px

// Gaps
gap: var(--spacing-3);        // 12px
```

### Bordes

```scss
// Border Radius
border-radius: var(--radius-sm);    // 4px
border-radius: var(--radius-md);    // 8px
border-radius: var(--radius-lg);    // 12px
border-radius: var(--radius-xl);    // 16px

// Borders
border: 1px solid var(--border-default);
```

### Sombras

```scss
box-shadow: var(--shadow-xs);   // Muy sutil
box-shadow: var(--shadow-sm);   // Pequeña
box-shadow: var(--shadow-md);   // Media
box-shadow: var(--shadow-lg);   // Grande
box-shadow: var(--shadow-xl);   // Extra grande
```

---

## ⚡ Tips y Mejores Prácticas

### 1. Siempre Usa Tokens

❌ **Evita:**
```scss
color: #333;
padding: 16px;
border-radius: 8px;
```

✅ **Prefiere:**
```scss
color: var(--text-primary);
padding: var(--spacing-4);
border-radius: var(--radius-md);
```

### 2. Standalone Components

✅ Siempre importa componentes directamente:
```typescript
imports: [ButtonComponent]
```

### 3. Type Safety

✅ Usa los tipos de TypeScript:
```typescript
import { ButtonSize, ButtonVariant } from '@mgomezw/bioma';

size: ButtonSize = 'large';
variant: ButtonVariant = 'filled';
```

### 4. Responsive Design

```scss
.container {
  padding: var(--spacing-4);

  @media (min-width: 768px) {
    padding: var(--spacing-8);
  }
}
```

---

## 🎯 Siguiente Paso

Ahora que tienes lo básico, explora:

👉 [Foundations - Aprende sobre Design Tokens](/docs/foundations-overview--docs)
👉 [Components - Explora todos los componentes](/docs/components-overview--docs)
👉 [Button - Componente de ejemplo completo](/docs/components-button--docs)
