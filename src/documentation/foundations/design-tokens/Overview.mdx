import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Bioma 2.1/Foundations/Design Tokens" />

# Design Tokens

Los Design Tokens son la base de Bioma 2.0. Son variables que almacenan valores de diseño reutilizables que pueden ser referenciados en todo el sistema.

---

## 🎯 ¿Qué son Design Tokens?

Los **Design Tokens** son pares nombre-valor que representan decisiones de diseño. En lugar de usar valores hardcoded como `#E30038` o `16px`, usamos tokens como `--primary-default` o `--spacing-4`.

### Beneficios

✅ **Consistencia**: Un solo cambio actualiza todo el sistema
✅ **Mantenibilidad**: Fácil de actualizar y escalar
✅ **Theming**: Soporte para múltiples temas
✅ **Documentación**: Los nombres describen el propósito
✅ **Type-safety**: Validación en tiempo de desarrollo

---

## 📊 Arquitectura de Tres Niveles

### Nivel 1: Foundation Tokens
Valores primitivos, la base del sistema.

```scss
// Colores primitivos
--brand-500: #E30038;
--neutral-900: #121212;

// Espaciado primitivo
--spacing-4: 16px;
--spacing-6: 24px;

// Radios primitivos
--radius-md: 8px;
```

### Nivel 2: Semantic Tokens
Dan significado contextual a los foundation tokens.

```scss
// Acciones
--primary-default: var(--brand-500);
--secondary-default: var(--neutral-700);

// Textos
--text-primary: var(--neutral-900);
--text-secondary: var(--neutral-600);

// Fondos
--bg-default: var(--neutral-100);
```

### Nivel 3: Component Tokens
Específicos para componentes.

```scss
// Botones
--button-bg-filled: var(--primary-default);
--button-padding-large: var(--spacing-3) var(--spacing-6);
```

---

## 🎨 Categorías de Tokens

### Color Tokens
- **Foundation**: 24 colores base (neutral + brand scales)
- **Semantic**: 47 tokens contextuales
- **States**: success, warning, error, info

[Ver sistema de colores](/docs/foundations-color--docs)

### Spacing Tokens
- 11 valores de espaciado (0px → 64px)
- Escala consistente
- Usados para padding, margin, gap

[Ver sistema de espaciado](/docs/foundations-spacing--docs)

### Typography Tokens
- Font families
- Font sizes (14 tamaños)
- Font weights (6 pesos)
- Line heights

[Ver sistema de tipografía](/docs/foundations-typography--docs)

### Border Tokens
- Border radius (8 valores)
- Border widths
- Border colors

[Ver sistema de bordes](/docs/foundations-border--docs)

### Shadow Tokens
- 5 niveles de elevación
- Sombras consistentes

[Ver sistema de elevación](/docs/foundations-elevation--docs)

### Animation Tokens
- Timing (3 velocidades)
- Easing functions (3 curvas)

[Ver sistema de animación](/docs/foundations-animation--docs)

### Z-Index Tokens
- 8 niveles de profundidad
- Jerarquía clara

---

## 🔧 Cómo Usar Tokens

### En CSS/SCSS

```scss
.mi-componente {
  // ✅ Correcto - Usa tokens
  color: var(--text-primary);
  background: var(--bg-default);
  padding: var(--spacing-4);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-sm);
  transition: all var(--transition-normal);

  // ❌ Incorrecto - Valores hardcoded
  color: #121212;
  background: #FFFFFF;
  padding: 16px;
  border-radius: 8px;
}
```

### En TypeScript

```typescript
// Leer valores de tokens
const getTokenValue = (tokenName: string): string => {
  return getComputedStyle(document.documentElement)
    .getPropertyValue(tokenName)
    .trim();
};

const primaryColor = getTokenValue('--primary-default');
// "#E30038"
```

### En Componentes Angular

```typescript
@Component({
  selector: 'my-component',
  styles: [`
    :host {
      padding: var(--spacing-4);
      background: var(--bg-default);
    }
  `]
})
```

---

## 🌗 Theming con Tokens

Los tokens permiten fácil soporte para múltiples temas:

```scss
// Light theme (default)
:root {
  --bg-default: #FFFFFF;
  --text-primary: #121212;
}

// Dark theme
[data-theme="dark"] {
  --bg-default: #121212;
  --text-primary: #FAFAFA;
}
```

**Activar tema:**

```html
<div data-theme="dark">
  <!-- Todo aquí usa el tema oscuro -->
</div>
```

---

## 📝 Nomenclatura de Tokens

### Estructura del Nombre

```
--{category}-{variant}-{state}
```

### Ejemplos

```scss
// Categoría: text, variant: primary
--text-primary

// Categoría: primary (acción), variant: default
--primary-default

// Categoría: primary, variant: hover
--primary-hover

// Categoría: bg (background), variant: subtle
--bg-subtle
```

### Reglas de Nomenclatura

✅ **Usa kebab-case**: `--text-primary` no `--textPrimary`
✅ **Sé descriptivo**: `--primary-default` no `--red`
✅ **Sigue el patrón**: categoría-variante-estado
✅ **Usa prefijos**: `--btn-` para componentes específicos

---

## 📊 Lista Completa de Tokens

### Foundation Tokens (68)

**Neutral Colors (13)**
```scss
--neutral-100, --neutral-200, ..., --neutral-900
```

**Brand Colors (11)**
```scss
--brand-100, --brand-200, ..., --brand-900
```

**Spacing (11)**
```scss
--spacing-0 (0px)
--spacing-1 (4px)
--spacing-2 (8px)
--spacing-3 (12px)
--spacing-4 (16px)
--spacing-5 (20px)
--spacing-6 (24px)
--spacing-8 (32px)
--spacing-10 (40px)
--spacing-12 (48px)
--spacing-16 (64px)
```

**Border Radius (8)**
```scss
--radius-none (0px)
--radius-xs (2px)
--radius-sm (4px)
--radius-md (8px)
--radius-lg (12px)
--radius-xl (16px)
--radius-2xl (24px)
--radius-full (9999px)
```

### Semantic Tokens (47)

**Text Colors (8)**
```scss
--text-primary
--text-secondary
--text-tertiary
--text-disabled
--text-link
--text-on-primary
--text-on-secondary
--text-inverse
```

**Background Colors (5)**
```scss
--bg-default
--bg-subtle
--bg-muted
--bg-elevated
--bg-overlay
```

**Border Colors (4)**
```scss
--border-default
--border-subtle
--border-strong
--border-inverse
```

**Primary Action (6)**
```scss
--primary-default
--primary-hover
--primary-active
--primary-disabled
--primary-subtle
--primary-strong
```

**Status Colors (16)**
```scss
// Success
--success-default, --success-hover, --success-subtle, --success-strong

// Warning
--warning-default, --warning-hover, --warning-subtle, --warning-strong

// Error
--error-default, --error-hover, --error-subtle, --error-strong

// Info
--info-default, --info-hover, --info-subtle, --info-strong
```

---

## 🚀 Mejores Prácticas

### 1. Siempre Usa Tokens

❌ **No hagas esto:**
```scss
.card {
  background: #FFFFFF;
  padding: 16px;
  border: 1px solid #E0E0E0;
}
```

✅ **Haz esto:**
```scss
.card {
  background: var(--bg-default);
  padding: var(--spacing-4);
  border: 1px solid var(--border-default);
}
```

### 2. Usa Semantic Tokens en Componentes

❌ **No uses foundation tokens directamente:**
```scss
.button {
  background: var(--brand-500);
}
```

✅ **Usa semantic tokens:**
```scss
.button {
  background: var(--primary-default);
}
```

### 3. Proporciona Fallbacks

```scss
.component {
  /* Fallback si el token no existe */
  color: var(--text-primary, #121212);
}
```

### 4. Documenta Tokens Personalizados

Si añades nuevos tokens, documéntalos:

```scss
/**
 * Custom component token
 * Used for: Special card highlight
 * Related to: --primary-subtle
 */
--card-highlight: var(--primary-subtle);
```

---

## 🎯 Próximos Pasos

Explora cada categoría de tokens en detalle:

- [Color Tokens](/docs/foundations-color--docs)
- [Typography Tokens](/docs/foundations-typography--docs)
- [Spacing Tokens](/docs/foundations-spacing--docs)
- [Border Tokens](/docs/foundations-border--docs)
- [Elevation Tokens](/docs/foundations-elevation--docs)
- [Animation Tokens](/docs/foundations-animation--docs)

---

## 📖 Referencias

- **Archivo fuente**: `src/styles/tokens.scss`
- **Total de tokens**: 150+
- **Temas soportados**: Light, Dark
- **Accesibilidad**: WCAG AA compliant
