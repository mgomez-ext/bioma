import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Radius" />

# Border Radius

El sistema de border radius define la personalidad visual con una escala de T-shirt sizing desde esquinas rectas hasta completamente redondeadas.

## Principios Clave

- **Progresión modular**: Incrementos de 4-8px
- **Consistencia**: Mismo redondeo para componentes similares
- **Flexibilidad**: Desde `none` hasta `full` (circular)

---

## Escala de Border Radius

| Token | Valor | Uso Principal |
|-------|-------|---------------|
| `radius/none` | `0px` | Tablas, divisores |
| `radius/sm` | `4px` | **Inputs, badges** |
| `radius/md` | `8px` | **Botones (estándar)** |
| `radius/lg` | `12px` | **Cards, modales** |
| `radius/xl` | `16px` | Cards grandes |
| `radius/2xl` | `24px` | Hero cards |
| `radius/3xl` | `32px` | Decorativo |
| `radius/full` | `999px` | **Avatares, pills** |

**Tokens más usados:**
- **radius/sm (4px)** - Inputs, badges, chips
- **radius/md (8px)** - Botones, controles estándar
- **radius/lg (12px)** - Cards, modales, popovers
- **radius/full (999px)** - Avatares circulares, pills

---

## Implementación en Código

### CSS Variables

```css
:root {
  --radius-none: 0px;
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-2xl: 24px;
  --radius-3xl: 32px;
  --radius-full: 999px;
}
```

### SCSS Variables

```scss
// Tokens de Radius
$radius-none: 0px;
$radius-sm: 4px;
$radius-md: 8px;
$radius-lg: 12px;
$radius-xl: 16px;
$radius-2xl: 24px;
$radius-3xl: 32px;
$radius-full: 999px;
```

### Uso en Componentes

```scss
// Inputs y controles pequeños
.input,
.badge,
.chip {
  border-radius: var(--radius-sm); // 4px
}

// Botones y controles estándar
.btn {
  border-radius: var(--radius-md); // 8px
}

// Cards y contenedores
.card,
.modal,
.dropdown {
  border-radius: var(--radius-lg); // 12px
}

// Elementos circulares
.avatar,
.pill {
  border-radius: var(--radius-full); // 999px
}
```

---

## Ejemplos Visuales

### Inputs (radius/sm - 4px)
```html
<input
  type="text"
  class="form-control"
  style="border-radius: var(--radius-sm);"
  placeholder="Texto con radius pequeño"
/>
```

### Botones (radius/md - 8px)
```html
<button
  class="btn btn-primary"
  style="border-radius: var(--radius-md);"
>
  Botón estándar
</button>
```

### Cards (radius/lg - 12px)
```html
<div
  class="card"
  style="border-radius: var(--radius-lg); padding: 24px;"
>
  <h3>Card Title</h3>
  <p>Contenido con radius grande</p>
</div>
```

### Avatares (radius/full - 999px)
```html
<img
  src="avatar.jpg"
  alt="Usuario"
  style="width: 48px; height: 48px; border-radius: var(--radius-full);"
/>
```

---

## Patrones de Uso

### Consistencia por Tipo de Componente

```scss
// ✅ CORRECTO - Mismo radius para controles similares
.input,
.select,
.textarea {
  border-radius: var(--radius-sm); // Todos los form controls usan SM
}

.btn,
.dropdown-toggle {
  border-radius: var(--radius-md); // Todos los botones usan MD
}

// ❌ INCORRECTO - Mezclar valores inconsistentemente
.input { border-radius: var(--radius-sm); }
.select { border-radius: var(--radius-lg); } // Inconsistente
```

### Radius Adaptativos

```scss
// Radius puede variar por tamaño de componente
.btn {
  border-radius: var(--radius-md);

  &.btn-sm {
    border-radius: var(--radius-sm); // Botón pequeño → radius pequeño
  }

  &.btn-lg {
    border-radius: var(--radius-lg); // Botón grande → radius grande
  }
}
```

---

## Migración y Estado Actual

### ✅ Completado
- Definición completa de escala de radius
- Implementación en Figma Variables
- Nomenclatura T-shirt sizing (none, sm, md, lg, xl, 2xl, 3xl, full)
- CSS Variables disponibles

### 🚧 En Proceso
- Conversión de componentes existentes
- Auditoría de valores no estándar en código legacy
- Documentación de patrones por tipo de componente

### ❌ Pendiente
- Semantic tokens para v3.0 (ej: `button/radius`, `card/radius`)
- Tokens responsive (radius reducido en mobile)
- Guidelines de radius por categoría de componente

---

## Guía de Decisión

**¿Qué radius usar?**

| Tipo de Componente | Radius Recomendado | Razón |
|-------------------|-------------------|-------|
| Input, Select, Textarea | `sm` (4px) | Controles pequeños con bordes sutiles |
| Badge, Chip, Tag | `sm` (4px) | Elementos compactos |
| Button, Toggle | `md` (8px) | Controles interactivos estándar |
| Card, Modal | `lg` (12px) | Contenedores grandes con presencia visual |
| Alert, Banner | `md` (8px) | Comunicación moderada |
| Avatar, Profile Picture | `full` (999px) | Elementos circulares |
| Pill Button | `full` (999px) | Botones con esquinas completamente redondeadas |

---

## Accesibilidad

- **No confiar solo en radius**: El border radius es decorativo, no debe ser el único indicador de interactividad
- **Contraste suficiente**: Asegurar contraste adecuado en bordes independientemente del radius
- **Focus visible**: Los estados de focus deben ser visibles con cualquier valor de radius
